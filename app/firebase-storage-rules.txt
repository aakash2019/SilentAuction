// Firebase Storage Rules Configuration
// Copy and paste these rules into your Firebase Console -> Storage -> Rules

/*
Rules for Firebase Storage:
Go to Firebase Console -> Storage -> Rules tab and replace the content with:

rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow authenticated users to upload/read items
    match /items/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
    
    // Allow authenticated admins to upload/read items (more restrictive)
    // match /items/{allPaths=**} {
    //   allow read: if request.auth != null;
    //   allow write: if request.auth != null && 
    //     exists(/databases/$(database)/documents/admin/$(request.auth.uid)) &&
    //     get(/databases/$(database)/documents/admin/$(request.auth.uid)).data.isAdmin == true;
    // }
  }
}

Note: Make sure your Firebase project has Storage enabled and the bucket is created.
You can enable Storage in Firebase Console -> Storage -> Get Started
*/
